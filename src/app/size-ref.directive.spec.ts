import { SizeRefDirective } from './size-ref.directive';

describe('SizeRefDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeRefDirective();
    expect(directive).toBeTruthy();
  });
});
